//
// File: fn_switch.sqf
// Author: Alduin86
//
// Description:
// Switch for pickaxe, netting and co
//
if(player distance (getMarkerPos "lead_1") < 50 && life_inv_pickaxe > 0) then
        {
           [] spawn life_fnc_pickAxeUse;
        };
        
if(player distance (getMarkerPos "iron_1") < 50 && life_inv_pickaxe > 0) then
        {
           [] spawn life_fnc_pickAxeUse;
        };
 
if(player distance (getMarkerPos "salt_1") < 50 && life_inv_pickaxe > 0) then
        {
           [] spawn life_fnc_pickAxeUse;
        };
 
if(player distance (getMarkerPos "sand_1") < 50 && life_inv_pickaxe > 0) then
        {
           [] spawn life_fnc_pickAxeUse;
        };
 
if(player distance (getMarkerPos "diamond_1") < 50 && life_inv_pickaxe > 0) then
        {
           [] spawn life_fnc_pickAxeUse;
        };
 
if(player distance (getMarkerPos "oil_1") < 50 && life_inv_pickaxe > 0) then
        {
           [] spawn life_fnc_pickAxeUse;
        };
        
if(player distance (getMarkerPos "oil_2") < 50 && life_inv_pickaxe > 0) then
        {
           [] spawn life_fnc_pickAxeUse;
        };
        
if(player distance (getMarkerPos "rock_1") < 50 && life_inv_pickaxe > 0) then
        {
           [] spawn life_fnc_pickAxeUse;
        };
 
